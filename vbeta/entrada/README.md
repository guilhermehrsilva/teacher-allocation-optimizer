# Entrada

Coloque aqui exatamente uma planilha `.xlsx` compatível com o contrato descrito em `../docs/contrato_entrada.md` e execute `python vbeta/executar_pipeline.py` a partir da raiz do repositório.

Arquivos temporários do Excel iniciados por `~$` são ignorados.
